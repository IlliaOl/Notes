version: '3.8'

services:
  webhost:
    image: lab_5
    build: .
    command: ./enterypoint.sh
    environment:
      - REDIS_HOST=redis_server
      - REDIS_PORT=6379
      - REDIS_DB=1
    volumes:
      - D:\dbdata:/app/database
    ports:
      - "8000:8000"
    depends_on:
      - redis_server
  redis_server:
    image: redis
    ports:
        - "6379:6379"
